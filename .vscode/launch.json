{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "[contest] Launch Chrome against localhost",
      "url": "http://localhost:3000/data-contest",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
