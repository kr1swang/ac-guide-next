import { makeStyles } from '@mui/styles'

const useStyles = makeStyles((theme) => ({
  root: {
    height: '100vh',
    width: '100vw',
    minWidth: '960px',
    display: 'flex',
    flexDirection: 'column'
  }
}))

export { useStyles }
